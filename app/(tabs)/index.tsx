import React from 'react';

// The path is correct (../../), but the filename must be all lowercase
import { ProductListingPage } from '../../features/product-listing-page/components/pages/productListingPage';

// Export the page as the default for this route
export default ProductListingPage;